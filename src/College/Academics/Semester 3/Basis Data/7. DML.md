# Insert
```sql
INSERT INTO users (id, name, email)
VALUES ('235150200111011', 'hohoho', NULL)
```
# Update
```sql
UPDATE users
SET email='ahargunyllib@student.ub.ac.id', name='Billy'
WHERE id='2351502001110111'
```
- kalo gada where bakal ke semua (be careful dik)
# Delete
```sql
DELETE FROM users
WHERE name='Billy'
```
# Insert into
```sql
INSERT INTO users2
SELECT * FROM users
```